import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.leo-plant.app",
  appName: "plant-app-client",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
